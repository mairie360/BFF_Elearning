name: BFF Elearning CICD

on:
  push:
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  CICD:
    uses: mairie360/CICD/.github/workflows/BFFs-cicd.yml@v1.7.5
    with:
      package_name: bff-elearning
      cicd_version: v1.7.5
      node_version: 20
    secrets: inherit
